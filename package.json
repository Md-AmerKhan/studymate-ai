{
  "name": "@caffeine/template-app",
  "type": "module",
  "engines": {
    "node": "20.x",
    "pnpm": ">=7.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "start": "serve src/frontend/dist -l $PORT",
    "typecheck": "pnpm -r --if-present run typecheck",
    "check": "pnpm -r --if-present run check",
    "fix": "pnpm -r --if-present run fix"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
